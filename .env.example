# CoreSkill Demo — Environment Variables
# Copy to .env and fill in as needed.
# The system works without any keys (Ollama local mode).

# OpenRouter (free tier available)
OPENROUTER_API_KEY=

# Anthropic (paid, optional)
ANTHROPIC_API_KEY=
